extends layout

block content
    h1 #{company.name}

    p
        a(href='/companies') Back to everyone

    if following.length
        p #{company.name} is following #{following.length} companies:
        // TODO should say 'company' if only one! ;)
        ul.companies
            for other in following
                li.company
                    form(action='/companies/#{company.id}/unfollow', method='POST')
                        a(href='/companies/#{other.id}') #{other.name}
                        input(type='hidden', name='company[id]', value='#{other.id}')
                        input(type='submit', class='unfollow', value='x')
    else
        p #{company.name} isn't following anyone currently.

    if others.length
        form(action='/companies/#{company.id}/follow', method='POST')
            p Add someone for #{company.name} to follow:
            label
                select(name='company[id]', required)
                    option(value='')
                    for company in others
                        option(value='#{company.id}') #{company.name}
            input(type='submit', value='Follow')
    else
        p There's no one else left for #{company.name} to follow!

    form(action='/companies/#{company.id}', method='POST')
        p Edit this company:
        input(type='text', name='name', placeholder='#{company.name}', required)
        input(type='submit', value='Update')

    form(action='/companies/#{company.id}', method='POST', onsubmit='return confirm("Are you sure?");')
        p And if you're feeling destructive…
        input(type='hidden', name='_method', value='DELETE')
        input(type='submit', value='Delete Company')
